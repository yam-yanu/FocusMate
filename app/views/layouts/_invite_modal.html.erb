<div class="modal fade" id="invite_modal">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<button class="close" data-dismiss="modal">&times;</button>
				<h4 class="modal-title"><i class="fa fa-envelope-o"></i> <%= current_user.group.name %>にメイトを招待</h4>
			</div>
			<form action="/send_mail" method="get" id="invite_form">
					<table class="modal-body table" style="width:100%; margin: 0;">
						<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
						<tbody>
							<tr>
								<td><span class="badge">宛先</span></td>
								<td>
									<input type="text" name="to_address" class="form-control input-sm" placeholder="example@example.com">
								</td>
							</tr>
							<tr>
								<td><span class="badge">件名</span></td>
								<td>
									<input type="text" name="subject" class="form-control input-sm" value="<%= "#{current_user.name}さんからFocusMateへのお誘い" %>">
								</td>
							</tr>
							<tr>
								<td><span class="badge">本文</span></td>
								<td style="text-align: center;">
									<textarea name="body" class="form-control detail_form" rows="3">FocusMateに参加しませんか?&#13;以下のURLにアクセスするとすぐに<%= current_user.name %>さんのグループに参加できます.</textarea>
									<input type="text" name="invite_address" class="form-control input-sm invite_address" value="<%= "#{root_url}group_id/#{current_user.group_id}/password/#{current_user.group.password}" %>" readonly onclick="this.select();"><br>
									(アドレスだけをLINE等に貼ることでも招待できます)
								</td>
							</tr>
						</tbody>
					</table>
				<div class="modal-footer">
					<button type="button" class="btn btn-primary invite_mail_btn">メール送信</button>
				</div>
			</form>
		</div>
	</div>
</div>