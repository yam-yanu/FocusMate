class Streamings
	dispatcher = new WebSocketRails("ws://#{location.host}/websocket")
	channel = dispatcher.subscribe("aaa")
	channel.bind "create", (tweet) ->
		alert(tweet)

window.TwitterStreamingSample ?= {}
window.TwitterStreamingSample.streamings = new Streamings